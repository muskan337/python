# `python` Folder

## Status

`python.py` in this folder is **empty**. It is a placeholder, so there is nothing to run or explain yet.

## Ideas for This File

Once you add code, a good first program is:

```python
print("Hello, World!")
```

Output:

```text
Hello, World!
```

Run it with:

```bash
python python.py
```

## Tip

Avoid naming a file `python.py` in real projects. It has the same name as the language and can cause confusion. Something like `hello.py` is clearer.
